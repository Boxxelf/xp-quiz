import React from "react";
import { createRoot } from "react-dom/client";
import XPQuiz from "./XPQuiz.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <XPQuiz />
  </React.StrictMode>
);
